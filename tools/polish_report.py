"""One-off: professional rewrite of abstract + section 7 with behaviour analysis."""

from pathlib import Path

p = Path("tools/make_report_v1.js")
t = p.read_text(encoding="utf-8")

# ---- 1. abstract ①: professional wording ----
old = '''P([T("① "), B("这个模型能做什么"), T("：输入水文站自动采集的原始分段流速（STIV 视频段 / 光流估计），直接输出整条断面全部测速线的表面流速——全自动、无人工定线。重建误差较「直接取原始观测」"),
           B("砍掉一半以上"), T("（RMSE 0.34–0.36 对 0.76 m/s），监测正常的测速线误差约 0.24–0.27 m/s，枯水断面自动判零，可疑数据自动红旗（约 6.6% 送人工复核）。")]),'''
new = '''P([T("① "), B("模型定位"), T("：以自动采集的原始分段流速观测（STIV 视频段 / 光流区域估计）为输入，直接重建整条断面全部测速线的表面流速，全程无需人工参与。站级测试集上重建 RMSE 0.34–0.36 m/s，较原始观测直接使用（0.76 m/s）降低过半；监测正常的测速线误差 0.24–0.27 m/s；枯水断面自动判零；模型-目标剧烈分歧断面自动标记（约占 6.6%）。")]),'''
assert old in t, "abstract-1 anchor missing"
t = t.replace(old, new)

# ---- 2. abstract ②: professional wording ----
old = '''P([T("② "), B("为什么要做它"), T("：平台的断面「目标」流速目前依赖人工/半自动整编（本次数据中 51% 的测速线为插值填充），而自动观测 7×24 海量产出却因单段噪声（直接使用误差 0.76 m/s）无法直接利用。模型证明：自动观测经去噪后可以可靠地重建断面流速——一次训练换取长期全自动产出，人工从「整编全部断面」缩减为「复核约 6.6% 的可疑线」。")]),'''
new = '''P([T("② "), B("研究价值"), T("：现行断面流速标准值依赖人工/半自动整编，本次数据中 51% 的测速线为插值填充；自动观测虽 7×24 持续产出，但单段噪声大（直接使用 RMSE 0.76 m/s）无法直接应用。本模型验证了从自动观测重建可靠断面流速的技术可行性：训练一次（约 8 小时单卡）即可持续产出，人工工作量由全量整编收敛至约 6.6% 的可疑断面复核。")]),'''
assert old in t, "abstract-2 anchor missing"
t = t.replace(old, new)

# ---- 3. polish colloquial phrase in conclusion 1 ----
old = "——纯 L2 让模型对「该 0 就 0、该大就大」的响应更忠实。"
new = "——纯 L2 下模型对零流速与高流速的重建均更忠实于观测证据。"
assert old in t, "conclusion-1 phrase missing"
t = t.replace(old, new)

# ---- 4. insert behaviour-analysis section, renumber ----
anchor = '  H2("7.3 方法学结论（对建模者）"),'
new_sections = '''  H2("7.3 模型行为分析：对矛盾与极端观测的响应"),
  P([T("对两版模型在三类特殊样本上的行为做了量化。三类行为共同表明：模型并非对任一信息源的二元服从，而是在人工目标与自动观测之间进行了"), B("稳健的加权融合"), T("。")]),
  TB([3000, 2000, 2000, 2360], [
    tbRow([tbCell("行为", { head: true, fill: "D9E2F3", w: 3000 }), tbCell("物理版", { head: true, fill: "D9E2F3", w: 2000 }), tbCell("纯净版", { head: true, fill: "D9E2F3", w: 2000 }), tbCell("样本量", { head: true, fill: "D9E2F3", w: 2360 })]),
    tbRow([tbCell("高流速压制：原始观测≥3 m/s 的线，重建均值/观测均值", { left: true }), tbCell("0.557"), tbCell("0.574"), tbCell("观测均值全部>3 m/s 的线", { left: true })]),
    tbRow([tbCell("零标定流速恢复：目标≤0.2 而观测≥0.5 的线，重建均值/观测均值", { left: true }), tbCell("0.441"), tbCell("0.406"), tbCell("52,978 条线", { left: true })]),
    tbRow([tbCell("矛盾线融合位置（0=目标，1=观测）中位数", { left: true }), tbCell("0.246"), tbCell("0.187"), tbCell("164,462 条线", { left: true })]),
  ]),
  P([B("行为一（高流速压制）："), T("对原始观测达到 3 m/s 以上的极端测速线（多数超出河流物理上限，属观测伪影），两版模型均将重建值压缩至观测的 56%–57%。该压缩对高流速观测伪影起到稳健估计的作用；对真实洪水测次则会引入低估，是 [3+) 段 RMSE 偏高的组成部分。")]),
  P([B("行为二（零标定断面的流速恢复）："), T("人工目标被标为近零（≤0.2 m/s）而原始观测显著（均值 2.01 m/s）的测速线达 52,978 条。两版模型在其中 91%–92% 的线上给出高于人工零标的重建值，且重建量级稳定为观测信号的 41%–44%。这表明模型并未盲从人工零标定，而是从观测中系统性地恢复了被抹去的流速信号。这 5.3 万条线是人工整编与自动观测分歧最集中的样本集——无论分歧根源是整编遗漏还是观测伪影，都具备明确的复核定标价值，且模型的重建值可作为复核的先验参考。")]),
  P([B("行为三（矛盾线的稳健折中）："), T("在观测与目标分歧的 164,462 条线上，重建值的位置分布为中位 19%–25%（0=目标，1=观测），四分位距 4%–61%——总体偏向目标一侧的加权折中，而非对任一信息源的二元取舍；个别断面上观测权重显著升高（如站 00156），构成自动识别矛盾样本的信号。")]),

  H2("7.4 方法学结论（对建模者）"),'''
assert anchor in t, "7.3 anchor missing"
t = t.replace(anchor, new_sections)

old = '  H2("7.4 当前边界（诚实声明）"),'
new = '  H2("7.5 后续工作"),'
assert old in t, "7.4 anchor missing"
t = t.replace(old, new)

old = '''P([T("高速段（≥3 m/s，占 1.2%）RMSE 0.85+，不宜直接使用；观测与目标矛盾的断面（约 10% 的线，集中在 2/37 个站，如站 00156 的 8 m/s 假观测）模型会被带偏，需人工判断；光流-only 测次建议叠加物理先验做二段校验。上述边界均有明确改进路径，属已知限制而非未知风险。")]),'''
new = '''P([T("① 高流速段：样本占比仅 1.2% 而 RMSE 达 0.85+，建议训练期按速度分层加权或过采样，并对高流速测次单独检验。② 矛盾线定标复核：52,978 条零标定-高观测矛盾线与约 10% 的分歧线建议人工复核定标，复核结果可反哺训练。③ 自适应物理先验：将物理正则由无条件附加改为随观测置信度调节，以兼得纯净版的总体精度与物理版在观测薄弱场景的稳健性。")]),'''
assert old in t, "7.5 content anchor missing"
t = t.replace(old, new)

p.write_text(t, encoding="utf-8")
print("polished OK")
