"""One-off: rewrite the abstract lead and section 7 of make_report_v1.js."""

from pathlib import Path

p = Path("tools/make_report_v1.js")
t = p.read_text(encoding="utf-8")

# 1. abstract: lead with capability + value, keep the ablation item as ③
old_1 = '''        P([T("① "), B("单一变量对照"), T("：两版模型同架构（82.76M）、同数据（218,306 断面）、同站级分层划分，仅两处不同——物理版的形态偏置注意力 + MSE+五项物理正则，对照版的 RoPE 位置编码 + 纯 L2。")]),
        P([T("② "), B("总体"), T("：纯净版全面占优——RMSE 0.342 对 0.364（好 6.0%），NSE 0.835 对 0.814，bias 0.023 对 0.056；对原始观测中位数基线的 skill 0.553 对 0.524。")]),
        P([T("③ "), B("误差结构")'''
new_1 = '''        P([T("① "), B("这个模型能做什么"), T("：输入水文站自动采集的原始分段流速（STIV 视频段 / 光流估计），直接输出整条断面全部测速线的表面流速——全自动、无人工定线。重建误差较「直接取原始观测」"),
           B("砍掉一半以上"), T("（RMSE 0.34–0.36 对 0.76 m/s），监测正常的测速线误差约 0.24–0.27 m/s，枯水断面自动判零，可疑数据自动红旗（约 6.6% 送人工复核）。")]),
        P([T("② "), B("为什么要做它"), T("：平台的断面「目标」流速目前依赖人工/半自动整编（本次数据中 51% 的测速线为插值填充），而自动观测 7×24 海量产出却因单段噪声（直接使用误差 0.76 m/s）无法直接利用。模型证明：自动观测经去噪后可以可靠地重建断面流速——一次训练换取长期全自动产出，人工从「整编全部断面」缩减为「复核约 6.6% 的可疑线」。")]),
        P([T("③ "), B("对照实验（单一变量）"), T("：两版模型同架构（82.76M）、同数据、同站级分层划分，仅两处不同——物理版的形态偏置注意力 + MSE+五项物理正则，对照版的 RoPE 位置编码 + 纯 L2。总体上纯净版全面占优：RMSE 0.342 对 0.364（好 6.0%），NSE 0.835 对 0.814，bias 0.023 对 0.056；对原始观测中位数基线的 skill 0.553 对 0.524。")]),
        P([T("④ "), B("方法学发现"),'''
assert old_1 in t, "abstract anchor not found"
t = t.replace(old_1, new_1)

# the old ④⑤ items now follow as continuation; renumber them into ④ tail & ⑤
old_4 = '''P([T("④ "), B("物理先验的守护价值"), T("：在观测薄弱的场景物理版反而更稳——零速枯水线 RMSE 0.199 对 0.252、光流-only 测次 RMSE 0.281 对 0.471。物理项充当了「观测不可信时的兜底先验」。")]),'''
new_4 = '''P([T("：在观测薄弱的场景物理版反而更稳——零速枯水线 RMSE 0.199 对 0.252、光流-only 测次 RMSE 0.281 对 0.471，物理项充当了「观测不可信时的兜底先验」。")]),
        P([T("⑤ "), B("共同短板"), '''
assert old_4 in t, "item4 anchor not found"
t = t.replace(old_4, new_4)

# 2. replace section 7 entirely
start = t.index('  H1("七、结论"),')
end = t.index('  H1("附录 A  评测口径"),')
new_7 = '''  H1("七、结论"),
  H2("7.1 这个模型能干什么"),
  P([T("给定一个断面任意时刻的原始分段流速观测（哪些线有观测、观测值多少，无需人工整理），模型输出该断面"), B("全部测速线的表面流速"),
     T("。实测能力（站级测试集，182 万条测速线）：")]),
  TB([3400, 5960], [
    tbRow([tbCell("能力", { head: true, fill: "D9E2F3", w: 3400 }), tbCell("实测水平", { head: true, fill: "D9E2F3", w: 5960 })]),
    tbRow([tbCell("有实测线的流速重建", { left: true }), tbCell("RMSE 0.26 m/s、NSE 0.92——可替代人工读数的精度", { left: true })]),
    tbRow([tbCell("无实测线的流速补全（占全部测速线 51%）", { left: true }), tbCell("RMSE 0.40 m/s——可用于趋势判断与异常筛查", { left: true })]),
    tbRow([tbCell("原始观测自动去噪", { left: true }), tbCell("原始观测直接使用误差 0.76 m/s，模型降至 0.34 m/s", { left: true })]),
    tbRow([tbCell("枯水自动判零", { left: true }), tbCell("枯水线（水深<1cm）重建值≈0，零速规则自动习得", { left: true })]),
    tbRow([tbCell("可疑数据自动红旗", { left: true }), tbCell("分歧>0.5 m/s 的线自动标出（占 6.6%），直接作为人工复核清单", { left: true })]),
  ]),
  H2("7.2 为什么值得做"),
  P([T("现状：断面流速的「标准值」靠人工/半自动整编——本次 21.8 万断面中 "), B("51% 的测速线为插值填充"),
     T("，人工整编慢、覆盖不全、本身带噪声；而自动观测设备 7×24 海量产出，却因单段噪声大（直接使用误差 0.76 m/s）无法利用。")]),
  P([T("本模型在两者之间搭桥："), B("把不可直接使用的自动观测变成可用的断面流速"),
     T("。一次训练（约 8 小时单卡）之后，每个断面无需人工步骤即可出全断面结果；人工工作量从「整编全部断面」缩减为「复核约 6.6% 的可疑线」。观测持续积累后模型可增量重训，收益持续放大。")]),
  H2("7.3 方法学结论（对建模者）"),
  P([B("结论 1（损失函数）："), T("物理正则是回归趋中的直接成因之一。纯净版总体 RMSE 好 6%、bias 减半、剧烈分歧率从 8.7% 降到 6.6%——纯 L2 让模型对「该 0 就 0、该大就大」的响应更忠实。")]),
  P([B("结论 2（物理先验的两面性）："), T("物理正则在观测充足时是拖累（把预测拉向均值），在观测薄弱时是兜底（枯水线、光流-only 显著更稳）。问题不是该不该要，而是该在哪起作用——作为无条件附加损失是错的，作为「观测置信度低时加大的自适应先验」可能是对的。")]),
  P([B("结论 3（共同瓶颈）："), T("两版最优验证 epoch 都停在早期（ep26/ep19）后进入长平台，且 [3+) 高速段 RMSE 均在 0.85+。瓶颈不在训练时长，而在高速样本稀少（占比 1.2%）与观测-目标矛盾样本。下一阶段：高速段过采样 / 分段损失加权 + 剧烈分歧集合人工复核。")]),
  H2("7.4 当前边界（诚实声明）"),
  P([T("高速段（≥3 m/s，占 1.2%）RMSE 0.85+，不宜直接使用；观测与目标矛盾的断面（约 10% 的线，集中在 2/37 个站，如站 00156 的 8 m/s 假观测）模型会被带偏，需人工判断；光流-only 测次建议叠加物理先验做二段校验。上述边界均有明确改进路径，属已知限制而非未知风险。")]),

'''
t = t[:start] + new_7 + t[end:]
p.write_text(t, encoding="utf-8")
print("rewritten OK")
